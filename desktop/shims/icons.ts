/**
 * Substitut de `lucide-react-native`.
 *
 * La version mobile dessine avec react-native-svg ; sur le bureau on prend la
 * version DOM du même jeu d'icônes. Les propriétés utilisées dans les écrans
 * (`color`, `size`, `strokeWidth`) portent les mêmes noms des deux côtés, donc
 * aucun appel n'est à retoucher.
 */
export * from 'lucide-react';
